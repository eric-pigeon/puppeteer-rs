#[derive(Serialize, Deserialize, Debug, Clone, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct {{id}} {
{{#each properties}}
    {{#if description}}
    // {{{comment description}}}
    {{/if}}
    {{> (propertyTypePartial this) objectId=../id}}
{{/each}}
}
